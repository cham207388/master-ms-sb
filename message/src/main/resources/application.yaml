server:
  port: 9010

spring:
  application:
    name: message
  cloud:
    function:
      definition: email|sms # combining multiple functions with pipe (|) separator as a single function definition