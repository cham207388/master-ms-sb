include:
  - docker-compose.infra.yml
  - docker-compose.db.yml
  - docker-compose-observability.yml
  - config-server/compose.yml
  - eureka-server/compose.yml
  - accounts/compose.yml
  - cards/compose.yml
  - loans/compose.yml
  - gateway-server/compose.yml

volumes:
  bank-pg-volume:

networks:
  securedbank:
    driver: bridge
